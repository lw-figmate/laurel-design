### AddressForm

**Import**: `import { AddressForm } from '@anthropic/laurel-design'`

Pre-built address input form.

**Example**:
```tsx
<AddressForm onSubmit={(values) => handleAddress(values)} defaultValues={{ country: 'US' }} />
```
