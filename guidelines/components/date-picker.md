### DatePicker

**Import**: `import { DatePicker } from '@anthropic/laurel-design'`

Calendar-based date selector.

**Example**:
```tsx
<DatePicker value={date} onChange={setDate} placeholder="Select a date" />
```
