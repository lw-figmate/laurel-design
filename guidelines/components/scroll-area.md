### ScrollArea

**Import**: `import { ScrollArea } from '@anthropic/laurel-design'`

Styled scrollable container with custom scrollbar.

**Example**:
```tsx
<ScrollArea style={{ height: 300 }}>
  <div>Long scrollable content...</div>
</ScrollArea>
```
