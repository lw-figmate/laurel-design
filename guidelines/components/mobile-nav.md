### MobileNav

**Import**: `import { MobileNav } from '@anthropic/laurel-design'`

Mobile navigation drawer.

**Example**:
```tsx
<MobileNav open={navOpen} onClose={() => setNavOpen(false)} links={[{ label: 'Home', href: '/' }, { label: 'Products', href: '/products' }]} />
```
