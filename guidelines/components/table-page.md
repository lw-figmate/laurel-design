### TablePage

**Import**: `import { TablePage } from '@anthropic/laurel-design'`

Full data table page with filters and pagination.

**Example**:
```tsx
<TablePage title="Users" columns={columns} data={users} filters={filterConfig} />
```
