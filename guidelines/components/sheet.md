### Sheet

**Import**: `import { Sheet } from '@anthropic/laurel-design'`

Bottom or side sheet overlay panel.

**Example**:
```tsx
<Sheet open={open} onClose={() => setOpen(false)} title="Filters">
  <Text>Sheet content</Text>
</Sheet>
```
