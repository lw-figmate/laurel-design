### Rating

**Import**: `import { Rating } from '@anthropic/laurel-design'`

Star rating input/display.

**Example**:
```tsx
<Rating value={4} max={5} onChange={setRating} />
<Rating value={3.5} readOnly />
```
