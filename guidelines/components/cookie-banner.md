### CookieBanner

**Import**: `import { CookieBanner } from '@anthropic/laurel-design'`

Cookie consent banner.

**Example**:
```tsx
<CookieBanner onAccept={() => acceptCookies()} onDecline={() => declineCookies()} />
```
