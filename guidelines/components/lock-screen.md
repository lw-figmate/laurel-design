### LockScreen

**Import**: `import { LockScreen } from '@anthropic/laurel-design'`

Lock/splash screen template.

**Example**:
```tsx
<LockScreen logo={<Text weight="bold" size="3xl">Acme</Text>} onUnlock={handleUnlock} />
```
