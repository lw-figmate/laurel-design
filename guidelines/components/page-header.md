### PageHeader

**Import**: `import { PageHeader } from '@anthropic/laurel-design'`

Page header with title, breadcrumbs, and actions.

**Example**:
```tsx
<PageHeader title="Settings" breadcrumbs={[{ label: 'Home', href: '/' }, { label: 'Settings' }]} actions={<Button>Save</Button>} />
```
