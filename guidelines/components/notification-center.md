### NotificationCenter

**Import**: `import { NotificationCenter } from '@anthropic/laurel-design'`

Notification list panel.

**Example**:
```tsx
<NotificationCenter items={notifications} onDismiss={(id) => dismiss(id)} onMarkRead={(id) => markRead(id)} />
```
