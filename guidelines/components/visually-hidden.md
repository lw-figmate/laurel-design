### VisuallyHidden

**Import**: `import { VisuallyHidden } from '@anthropic/laurel-design'`

Screen reader only content (visually hidden but accessible).

**Example**:
```tsx
<Button>
  <Icon name="search" size="md" />
  <VisuallyHidden>Search</VisuallyHidden>
</Button>
```
