### CheckboxGroup

**Purpose**: Group of related checkboxes with shared label.

**Import**: `import { CheckboxGroup } from '@anthropic/laurel-design'`

**Example**:
```tsx
<CheckboxGroup label="Interests" options={['Design', 'Engineering', 'Product']} value={selected} onChange={setSelected} />
```
