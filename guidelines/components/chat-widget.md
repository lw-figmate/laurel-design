### ChatWidget

**Import**: `import { ChatWidget } from '@anthropic/laurel-design'`

Chat message interface.

**Example**:
```tsx
<ChatWidget messages={messages} onSend={(text) => handleSend(text)} />
```
