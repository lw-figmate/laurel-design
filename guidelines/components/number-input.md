### NumberInput

**Import**: `import { NumberInput } from '@anthropic/laurel-design'`

Numeric input with increment/decrement buttons.

**Example**:
```tsx
<NumberInput min={0} max={100} step={1} value={quantity} onValueChange={setQuantity} />
```
