### Blockquote

**Import**: `import { Blockquote } from '@anthropic/laurel-design'`

Styled quotation block.

**Example**:
```tsx
<Blockquote accent="primary" size="lg">
  "Design is not just what it looks like, design is how it works."
</Blockquote>
```
