### CommandBar

**Import**: `import { CommandBar } from '@anthropic/laurel-design'`

Global search/command bar.

**Example**:
```tsx
<CommandBar open={open} onClose={() => setOpen(false)} onSelect={handleSelect} />
```
